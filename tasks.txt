tasks:
grab id and date from flickr images
sort images by date
allow user to click on face
bio and image screen

for each photo in iss album:
	grab the id

ids = [520, 521]

	display image[0] from id's grabbed earlier
	display image associated with that id
	for the current photo, do face recognition
	if user clicks face:
		build timeline
			for astronaut clicked, grab id's of images they're in
			get the date taken for each image
			build image timeline based off date
		show name, info, picture timeline

imageDates
<imageID, dateTaken>
need function that sorts dictionary entries by value

how to register clicks within python
	on hover, highlight the box around the person's face
	on click, 





	